package com.example.lld.inventoryManagement.enums;

public enum OrderStatus {
    PLACED,
    CONFIRMED,
    PROCESSING,
    PACKED,
    SHIPPED,
    OUT_FOR_DELIVERY,
    DELIVERED,

    CANCELLED,
    RETURN_REQUESTED,
    RETURNED,
    REFUNDED,

    FAILED
}